import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Comment has message', (tester) async {
    //   await tester.pumpWidget(buildTestableWidget(Comment()));
    //   final textFinder = find.text('Comment');
    //   expect(textFinder, findsOneWidget);
  });
}
