import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CommentTile has message', (tester) async {
    //   await tester.pumpWidget(buildTestableWidget(CommentTile()));
    //   final textFinder = find.text('CommentTile');
    //   expect(textFinder, findsOneWidget);
  });
}
