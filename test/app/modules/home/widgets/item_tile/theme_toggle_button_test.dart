import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ThemeToggleButton has message', (tester) async {
    //   await tester.pumpWidget(buildTestableWidget(ThemeToggleButton()));
    //   final textFinder = find.text('ThemeToggleButton');
    //   expect(textFinder, findsOneWidget);
  });
}
