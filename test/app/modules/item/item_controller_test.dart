import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:zone/app/modules/item/item_module.dart';

void main() {
  initModule(ItemModule());
  // ItemController item;
  //
  setUp(() {
    //     item = ItemModule.to.get<ItemController>();
  });

  group('ItemController Test', () {
    //   test("First Test", () {
    //     expect(item, isInstanceOf<ItemController>());
    //   });

    //   test("Set Value", () {
    //     expect(item.value, equals(0));
    //     item.increment();
    //     expect(item.value, equals(1));
    //   });
  });
}
