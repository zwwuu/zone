import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CommentTree has message', (tester) async {
    //   await tester.pumpWidget(buildTestableWidget(CommentTree()));
    //   final textFinder = find.text('CommentTree');
    //   expect(textFinder, findsOneWidget);
  });
}
