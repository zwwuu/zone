import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ItemTileSubtitle has message', (tester) async {
    //   await tester.pumpWidget(buildTestableWidget(ItemTileSubtitle()));
    //   final textFinder = find.text('ItemTileSubtitle');
    //   expect(textFinder, findsOneWidget);
  });
}
